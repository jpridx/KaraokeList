@inherits LayoutComponentBase
@using Microsoft.AspNetCore.Components.Authorization
@inject NavigationManager NavigationManager
@inject JwtAuthenticationStateProvider AuthProvider

<div class="app-shell">
    <header class="app-header">
        <a class="app-brand" href="">My Karaoke</a>

        <AuthorizeView>
            <Authorized>
                <nav class="app-header-nav d-none d-md-flex" aria-label="Primary">
                    <NavLink class="app-header-link" href="log" Match="NavLinkMatch.Prefix">Log</NavLink>
                    <NavLink class="app-header-link" href="my-songs" Match="NavLinkMatch.Prefix">My Songs</NavLink>
                    <NavLink class="app-header-link" href="more" Match="NavLinkMatch.Prefix">More</NavLink>
                </nav>
                <div class="app-header-actions ms-auto">
                    <span class="app-user d-none d-lg-inline text-muted small">@context.User.Identity?.Name</span>
                    <button type="button" class="btn btn-sm btn-outline-secondary" @onclick="LogoutAsync">Sign out</button>
                </div>
                <nav class="mobile-top-nav d-md-none w-100" aria-label="Primary">
                    <NavLink class="mobile-top-nav-link" href="log" Match="NavLinkMatch.Prefix">Log</NavLink>
                    <NavLink class="mobile-top-nav-link" href="my-songs" Match="NavLinkMatch.Prefix">My Songs</NavLink>
                    <NavLink class="mobile-top-nav-link" href="my-performances" Match="NavLinkMatch.Prefix">Perf.</NavLink>
                    <NavLink class="mobile-top-nav-link" href="more" Match="NavLinkMatch.Prefix">More</NavLink>
                </nav>
            </Authorized>
            <NotAuthorized>
                <div class="app-header-actions ms-auto">
                    <a class="btn btn-sm btn-outline-primary" href="login">Sign in</a>
                    <a class="btn btn-sm btn-primary" href="register">Register</a>
                </div>
            </NotAuthorized>
        </AuthorizeView>
    </header>

    <main class="app-main">
        <article class="app-content px-3 px-md-4">
            <AppUpdateNotice />
            <SlowApiNotice />
            <AuthorizeView>
                <Authorized>
                    <PendingPerformancesNotice />
                </Authorized>
            </AuthorizeView>
            @Body
        </article>
    </main>
</div>

@code {
    private async Task LogoutAsync()
    {
        await AuthProvider.MarkUserAsLoggedOutAsync();
        NavigationManager.NavigateTo("/login");
    }
}
