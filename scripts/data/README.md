# Legacy SQLite source (optional)

Place `Karaoke.sqlite3` here when using `scripts/MigrateSqliteToSqlServer`, or pass the file path as the first argument to that tool.

The bundled SQLite database that lived under the removed Blazor Server project is no longer in the repo. Use [seed-catalog.sql](../seed-catalog.sql) for a fresh catalog seed instead.
